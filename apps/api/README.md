# Game Arena API

```bash
cp .env.example .env
npm run dev
```

The development adapter is in-memory. Production deployment requires PostgreSQL persistence and live OTP/JazzCash provider adapters before traffic is enabled.

Run `npm run ci` for syntax, schema and contract tests.
