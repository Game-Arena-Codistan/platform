import {randomUUID} from 'node:crypto';
const SECRET_KEYS=/password|secret|token|cookie|authorization|otp|code|signature|card|cnic/i;
export function redact(value,depth=0){if(depth>5)return'[truncated]';if(Array.isArray(value))return value.slice(0,100).map(item=>redact(item,depth+1));if(value&&typeof value==='object'){const result={};for(const [key,item] of Object.entries(value))result[key]=SECRET_KEYS.test(key)?'[redacted]':redact(item,depth+1);return result;}if(typeof value==='string'&&value.length>500)return`${value.slice(0,500)}…`;return value;}
export class AuditLog{constructor(){this.events=[];}write({actor='system',action,targetType,targetId,requestId,metadata={}}){const event={id:randomUUID(),at:new Date().toISOString(),actor,action,targetType,targetId,requestId,metadata:redact(metadata)};this.events.push(event);return event;}search({actor,action,targetType,targetId,limit=100}={}){return this.events.filter(item=>(!actor||item.actor===actor)&&(!action||item.action===action)&&(!targetType||item.targetType===targetType)&&(!targetId||item.targetId===targetId)).slice(-Math.min(limit,500)).map(item=>structuredClone(item));}}
export class Metrics{constructor(){this.counters=new Map();this.timings=new Map();}increment(name,labels={},value=1){const key=`${name}:${JSON.stringify(labels,Object.keys(labels).sort())}`;this.counters.set(key,(this.counters.get(key)||0)+value);}observe(name,value,labels={}){const key=`${name}:${JSON.stringify(labels,Object.keys(labels).sort())}`;const list=this.timings.get(key)||[];list.push(Number(value)||0);if(list.length>1000)list.shift();this.timings.set(key,list);}snapshot(){return{counters:Object.fromEntries(this.counters),timings:Object.fromEntries([...this.timings].map(([key,values])=>[key,{count:values.length,min:Math.min(...values),max:Math.max(...values),avg:values.reduce((a,b)=>a+b,0)/values.length}]))};}}
