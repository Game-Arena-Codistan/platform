import {supportTopics} from '../data.js';
import {submitSupportTicket} from '../api.js';
import {escapeHtml,toast} from '../ui.js';
import {track} from '../analytics.js';
export function renderSupport(){return `<section class="page"><header class="page-head"><div><span class="eyebrow">Help & support</span><h1>How can we help?</h1><p>Send a structured support request without sharing passwords, OTPs or wallet PINs.</p></div></header><div class="split wide"><form id="support-form" class="card card-body stack"><div class="field"><label for="support-topic">Topic</label><select id="support-topic" name="topic">${supportTopics.map(item=>`<option>${escapeHtml(item)}</option>`).join('')}</select></div><div class="field"><label for="support-message">What happened?</label><textarea id="support-message" name="message" maxlength="1500" required placeholder="Include the game, screen, date and any error message."></textarea></div><div class="field"><label for="support-reference">Optional reference</label><input id="support-reference" name="reference" maxlength="100" placeholder="Transaction, game or tournament ID"></div><button class="button primary" type="submit">Submit request</button><p id="support-status" role="status"></p></form><aside class="stack"><article class="card card-body"><h3>Game not loading</h3><p>Try another network, disable data saver for that game and send the exact title and error.</p></article><article class="card card-body"><h3>Payment pending</h3><p>Do not pay twice. Keep the transaction reference and wait for server confirmation.</p></article><article class="card card-body"><h3>Account access</h3><p>Use the same verified mobile number or email. Never share the OTP with support.</p></article></aside></div></section>`;}
export function bindSupport(){document.querySelector('#support-form')?.addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget;const payload=Object.fromEntries(new FormData(form));const status=document.querySelector('#support-status');status.textContent='Submitting…';try{const result=await submitSupportTicket(payload);track('support_submit',{topic:payload.topic});status.textContent=`Request ${result.ticket.id} received.`;form.reset();toast('Support request submitted');}catch(error){status.textContent=error.message;}});}
