const release=new URL(self.location.href).searchParams.get('release')||'dev';
const CACHE=`game-arena-shell-${release}`;
const versioned=path=>`${path}?v=${encodeURIComponent(release)}`;
const SHELL=['/','/index.html','/manifest.webmanifest','/assets/icon.svg','/assets/logo.svg','/demo-games/arena-dash/index.html',versioned('/styles/tokens.css'),versioned('/styles/app.css'),versioned('/styles/responsive.css'),versioned('/src/app.js')];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(SHELL)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE&&!key.startsWith('game-arena-downloads')).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{const request=event.request;if(request.method!=='GET')return;const url=new URL(request.url);if(url.origin!==location.origin)return;if(url.pathname==='/config.js'||url.pathname==='/sw.js'){event.respondWith(fetch(request,{cache:'no-store'}));return;}if(request.mode==='navigate'){event.respondWith(fetch(request,{cache:'no-store'}).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put('/index.html',copy));return response;}).catch(()=>caches.match('/index.html')));return;}event.respondWith(caches.match(request).then(cached=>cached||fetch(request).then(response=>{if(response.ok&&['style','script','image','manifest','document'].includes(request.destination)){const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(request,copy));}return response;})));});
